{
  "name": "llm-news-report",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "python3 -m http.server 8080",
    "build": "echo 'Static site - no build needed'",
    "deploy": "git push origin main"
  }
}